---
id: T-005
title: Correct the stale README badges
type: task
tier: routine
requestor: lionel
advisors: []
---

## Problem

`README.md:4` badges `python-3.8`; `pyproject.toml` requires `>=3.13`.
`README.md:6` badges the black code style and links `psf/black`; the project
runs ruff (`target-version = "py313"`, line-length 100) and has no black
anywhere. Both are hardcoded shields.io strings, so nothing tripped when the
underlying facts moved.

The rest of the README is current — the hosting section went with T-004, the
OAuth link and the `.env` path were refreshed under T-004 and T-001. These two
badges are the only stale claims left in it.

## Outcome

The README states what is true: Python 3.13, no code-style claim. One hardcoded
badge remains, deliberately.

## Grilling record

Lionel, live, one round — three options put to him (drop both, update both,
keep Python and drop style). Chose **keep Python, drop style**: a contributor
genuinely needs the version floor, and the style badge is decoration. Halves
the drift surface while keeping the badge that carries information.

Section **Now**, tier **routine** — no new surface, no dependency or runtime
floor change, not a constitutional doc.

Advisors were not spawned: this session is configured not to launch subagents
unless asked. The eng/pm analysis was done in the main loop instead, and the
options put to Lionel came from it.

## Tasks

- `README.md:4`: badge 3.13, matching `requires-python`.
- `README.md:6`: delete the code-style badge and its `psf/black` link.
- `README.md:26`: `paramater` → `parameter`, found while reading the file.
- Record the standing constraint in `refs/eng/INDEX.md`: a hardcoded badge that
  restates a `pyproject.toml` fact drifts with no trigger to catch it — this
  pair proved it. The Python floor is the accepted exception; do not add
  others.
- Verify: `just quality` green, and no `3.8`/`black` left in `README.md`.
