---
id: T-016
title: Slim refs/eng/INDEX.md
type: task
tier: routine
requestor: lionel
advisors: [harness]
---

## Problem

`refs/eng/INDEX.md` is 3393 words against `refs/pm/INDEX.md`'s 224, and `eng` loads it on every spawn.

Lines 20–29 and 37 are 1338 of those words — 39% of the file — re-narrating the trail encoding, the expiry loop and the claim race. `.claude/rules/bot-code.md` (821 words) already owns those invariants and loads automatically whenever the bot code is touched, so `eng` pays for both.

Not the cause, checked: the `(Lionel, T-NNN)` standing-constraint lines. Those are one-liners carrying a decider and a why (L38 card fixture, L43 PyPI squat, L54 one artifact), and they are what stops a settled rejection being re-proposed.

## Outcome

`eng/INDEX.md` states what `eng` needs before it opens anything else, and points at `bot-code.md` for editing invariants rather than restating them.

## Advisor input

`harness`: duplication is the disease; one fact, one home.

## Grilling record

Lionel, 2026-07-29: scoped to `eng/INDEX.md`, not a full `refs/` sweep — pm and harness INDEXes are already short. Work it this session.

## Tasks

1. Cut L20–29 and L37 to what `bot-code.md` does not say. Keep the *why* that is architecture (why the stack lives in `custom_id`, why idleness not age); move or drop the mechanics an editor gets from the rule.
2. Keep every `(Lionel, T-NNN)` constraint line intact.
3. Verify: no fact deleted that exists in neither file afterwards — diff the two before and after.
