---
id: T-013
title: Filing is bookkeeping — delete /ticket step 7
type: task
tier: strategic
requestor: lionel
advisors: [harness]
---

## Problem

`/ticket` step 7 makes a filing write standing constraints into `refs/` or `.claude/rules/`. Two costs:

- It pulls the session-end audit into a filing. `audit-check.sh` watches `CLAUDE.md refs .claude`; `tickets/` and `PLAN.md` are not watched, so without step 7 a filing trips nothing.
- At filing time the constraint's home is a guess. It is known once the work is done.

The pre-commit review was never part of this: CLAUDE.md exempts plan bookkeeping, and the review on `804b91f` fired on a `src/krcg_bot/__init__.py` edit folded into that commit.

## Outcome

A filing commit touches `tickets/` and `PLAN.md` only, and fires no review and no audit.

## Advisor input

`harness`: conditional — filing is already review-exempt; the audit is the pass that caught what advisors and review missed (`9b794b7`), so cut the trigger, not the audit.

## Grilling record

Lionel, 2026-07-29: delete step 7. A constraint lands in the doc it governs at ship or drop time. Signed off live on the plan-format meaning-change; lands `todo`, not `awaiting-sign-off`.

Rejected: keeping step 7 for rejections only — still puts a `refs/` write in a filing commit.

## Tasks

1. `SKILL.md`: delete step 7. Step 6 states a filing commit touches `tickets/` + `PLAN.md` only.
2. `plan-format.md` §Lifecycle: a constraint settled during grilling lands in the doc it governs when the ticket ships or is dropped — never at filing.
3. Verify: file a ticket, confirm the Stop hook does not fire.
