---
id: T-014
title: Cap the prose — tickets, commit bodies, reports
type: task
tier: strategic
requestor: lionel
advisors: [harness]
---

## Problem

The harness's only word budget is `advisor replies ≤150 words`, and it holds. Nothing caps what the budget then feeds:

- Ticket bodies run 45–48 lines (`T-011`, `T-012`); `T-012`'s Problem is four paragraphs of incident narrative.
- Commit bodies run 10–15 lines restating the ticket they point at. One fact, two homes.
- Nothing governs reporting back to Lionel.

## Outcome

A ticket states the defect and its evidence; the commit points at it; a report leads with the answer.

## Advisor input

`harness`: real bloat is duplication, not length. Warns a prose budget is unenforceable by its own convention ("a rule that must hold regardless of judgement is a hook, not prose").

## Grilling record

Lionel, 2026-07-29: prose budget in `plan-format.md`, not a hook — the ≤150-word cap shows a small budget loaded early does hold. All four sections shrink. Signed off live on the constitutional edits; lands `todo`.

## Tasks

1. `plan-format.md`: body budget. Problem = defect + evidence, not narrative. Advisor input = verdict + binding conditions. Grilling record = what was settled and by whom. Whole body ≤40 lines.
2. `plan-format.md` §Commit discipline: the tombstone subject line is the message; a body only for what the ticket does not say.
3. `CLAUDE.md` §Style: reports lead with the answer; evidence over narrative.
4. Verify: T-013–T-016 bodies are already inside the budget.
