# Decisions in force

Log only what current state cannot express: rejections with reasons, standing constraints, not-yet-embodied decisions. Whoever embodies a decision deletes its line in the same commit, citing the new home. A reversed decision is deleted or replaced, never annotated.

- 2026-07-25 — Advisors (`pm`, `eng`) keep full tools and the inherited model: they are dual-role, consulted by `/ticket` and authors of their own `refs/`. Revisit if an advisor is ever caught editing bot code.
- 2026-07-25 — `/ticket` stays model-invocable despite its side effects (writes a ticket, commits). The gate rule is worth more than the protection: a skill Claude cannot reach is a gate agents route around. Revisit on the first unwanted auto-filing.
