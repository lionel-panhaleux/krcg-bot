# Whitelist only the recipients list and the age-encrypted secrets, so a
# plaintext password or token (or any other stray) can never be committed here.
*
!.gitignore
!age-recipients.txt
!vault-pass.age
!dev-env.age
